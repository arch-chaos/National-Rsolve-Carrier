# Generate strong secrets:
#   python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-me-to-a-random-secret
JWT_SECRET_KEY=change-me-to-another-random-secret

# Render auto-sets DATABASE_URL when you add PostgreSQL
# Leave as sqlite for local dev
DATABASE_URL=sqlite:///nrc_tms.db

# Your frontend URL (Render sets this automatically for Vercel)
FRONTEND_URL=http://localhost:5173
